{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}
{
  "scripts": {
    "build": "react-scripts build --legacy-peer-deps"
  }
}
{
  "scripts": {
    "build": "react-scripts build"
  }
}
